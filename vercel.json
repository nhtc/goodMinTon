{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build && prisma migrate deploy ",
  "installCommand": "npm install",
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}